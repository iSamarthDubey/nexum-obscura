{
  "name": "nexum-obscura-backend",
  "version": "1.0.0",
  "description": "Backend API for Nexum Obscura cybersecurity log analysis platform",
  "main": "minimal.js",
  "scripts": {
    "start": "node minimal.js",
    "dev": "nodemon minimal.js",
    "build": "npm install && mkdir -p shared && cp ../shared/*.csv shared/ || true",
    "test": "jest"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "csv-parser": "^3.0.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "moment": "^2.29.4",
    "mongoose": "^7.5.0",
    "multer": "^2.0.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.0.1"
  }
}
